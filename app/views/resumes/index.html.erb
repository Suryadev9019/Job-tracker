<h1 class="text-2xl font-bold mb-4">My Resumes</h1>

<%= link_to "➕ Upload New Resume", new_resume_path, class: "bg-blue-600 text-white px-3 py-2 rounded" %>

<ul class="mt-4 space-y-2">
  <% @resumes.each do |resume| %>
    <li class="border p-3 rounded">
      <%= link_to resume.file.filename.to_s, resume_path(resume), class: "text-blue-700 underline" %>
    </li>
  <% end %>
</ul>
